spring:
  datasource:
    url: jdbc:h2:file:./coffeeshop.h2;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
    username: cisc191
    password: honors
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop